import React from 'react';
import { NavLink } from 'react-router-dom';

function Header(props) {
    return (
        <div>
            {/* tutaj linki navigacja i logo */}
            header
        </div>
    );
}

export default Header;